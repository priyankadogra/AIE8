# API Configuration
# Replace with your deployed backend URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Note: API keys should be entered in the frontend UI for security
# Never commit actual API keys to version control

